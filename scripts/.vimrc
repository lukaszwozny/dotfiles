map ;; $a;
map ,, $a,
map <C-A> gg<S-V><S-G>

set number
set background=dark
syntax on

let &t_SI = "\<esc>[5 q"
let &t_SR = "\<esc>[3 q"
let &t_EI = "\<esc>[2 q"

" call plug#begin('~/.vim/plugged')
" Plug 'vim-scripts/yankring.vim'
" call plug#end()
